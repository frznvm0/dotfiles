[core]
	quotepath = false
	editor = vim
	autocrlf = input
[alias]
	co = checkout
	st = status -sb
	l = log --oneline --decorate --graph
	unstage = reset HEAD --
	# tee is needed because git appends arguments to the main command
	open = !open https://github.com/$(git remote -v |\
		sed -r 's/.*\\.com(:|\\/)(\\w+\\/\\w+).*/\\3/' | head -1)/$1 |\
		tee
[color]
	diff = auto
	status = auto
	branch = auto
	ui = always
[push]
	default = simple
